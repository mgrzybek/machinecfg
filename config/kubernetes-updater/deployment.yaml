---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-updater
  namespace: machinecfg-system
  labels:
    app: kubernetes-updater
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kubernetes-updater
  template:
    metadata:
      labels:
        app: kubernetes-updater
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: /metrics
    spec:
      serviceAccountName: kubernetes-updater
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      containers:
        - name: controller
          image: machinecfg-controller-kubernetes-updater:latest
          command: ["/bin/machinecfg-controller-kubernetes-updater"]
          ports:
            - name: metrics
              containerPort: 8080
              protocol: TCP
            - name: health
              containerPort: 8081
              protocol: TCP
          env:
            - name: CONTROLLER_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: LEADER_ELECT
              value: "true"
            # Autotune: GOMAXPROCS is set from the container CPU limit.
            - name: LIMITS_CPU
              valueFrom:
                resourceFieldRef:
                  containerName: controller
                  resource: limits.cpu
                  divisor: "1"
            - name: LIMITS_MEMORY
              valueFrom:
                resourceFieldRef:
                  containerName: controller
                  resource: limits.memory
                  divisor: "1Mi"
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: "1"
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
