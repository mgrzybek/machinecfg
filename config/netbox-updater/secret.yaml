---
# Placeholder Secret — replace netbox_token with the actual value in your overlay.
# Never commit real tokens; use a Sealed Secret or external-secrets operator.
apiVersion: v1
kind: Secret
metadata:
  name: netbox-updater-secret
  namespace: machinecfg-system
stringData:
  netbox_token: REPLACE_ME
